# 工作階段存檔 — 2026-05-30

> 由 agent 自動產生，記錄 FXCanvas 專案在該日期的 git 狀態與進行中工作，方便之後接續或對照。

## 本機開發

| 項目 | 值 |
|------|-----|
| 專案路徑 | `/Users/n1/Documents/effect-clone` |
| Dev server | `npm run dev` → http://localhost:5173/ |
| 分支 | `main`（相對 `origin/main` **ahead 2**，尚未 push 最新 2 個 commit） |

## 已提交（尚未 push）

### `28005ad` — MSX ASCII + 影片

- MSX ASCII GLSL（多 palette、字元模式）
- 影片載入：`loadVideoFile()`、`updateVideoFrame()`、Canvas 播放控制
- `+page.svelte` 接受 `video/*`

### `3a3dbda` — 動畫時間軸與匯出

- `Timeline.svelte`、`animation.ts`、`keyframes.ts`、`keyframeEngine.ts`
- `animationExport.ts`（含 WebM）
- EffectPanel **ANIMATED** 區塊
- MSX ASCII 動畫模式、`u_time` 靜態圖動效
- 文件：`docs/visible-effects.md` 更新

## 工作區未提交（WIP）

約 **+708 / −80** 行，11 個已追蹤檔 + 未追蹤新模組：

### 已修改

- `Canvas.svelte` — 畫布／預覽行為調整
- `EffectPanel.svelte` — 面板行為
- `ExportMenu.svelte` — 匯出 UI／流程
- `animationExport.ts` — 動畫匯出擴充
- `renderer.ts` — 渲染小改
- `editor.ts` — 編輯器 store
- `+page.svelte` — 首頁（含 explore deep link `?effect=`）
- 四語系 `i18n/messages/*.ts`

### 未追蹤（新功能草稿）

| 路徑 | 用途 |
|------|------|
| `src/routes/explore/+page.svelte` | **Explore** 瀏覽頁（效果目錄、搜尋、分組） |
| `src/lib/explore/catalog.ts` | Explore 效果清單與篩選 |
| `src/lib/components/ExploreEffectCard.svelte` | 卡片元件 |
| `src/lib/samples/catalog.ts` | 範例素材目錄 |
| `src/lib/components/SampleCreditBar.svelte` | 素材 credit 列 |
| `src/lib/stores/exportSession.ts` | 匯出中暫停 canvas 預覽 loop |
| `static/samples/syouyu-biomech.jpg` | 範例圖 |

### 接續建議

1. 跑 `npm run test` 確認動畫／匯出／explore 路由
2. 補齊 explore 相關 i18n key（若尚未在 diff 內）
3. 滿意後：`git add` → commit → `git push origin main`
4. 發版時更新 `CHANGELOG.md` 並打 tag

## 應用內「存檔」機制（無需 agent）

| 機制 | 儲存位置 | 說明 |
|------|----------|------|
| **工作階段自動儲存** | `localStorage` + IndexedDB `fxcanvas` | 有圖時約 600ms debounce 存圖層堆疊；重開 `/` 自動 `restoreSession()`（除非 URL 有 `?effect=`） |
| **使用者 Preset** | `localStorage` `fxcanvas-presets-v1` | Presets 選單手動儲存／載入堆疊 |
| **面板寬度／收合** | `localStorage` | `fx-effect-panel-*` keys |
| **影片** | 不自動 session 存檔 | `saveSession()` 對 `HTMLVideoElement` 直接 return |

實作：`src/lib/stores/session.ts`，於 `+page.svelte` `onMount` 呼叫 `initSessionAutosave()`。

## 參考

- 產品對照：[effect.app](https://effect.app/)
- 倉庫文件：`docs/effect-app-reference.md`、`docs/visible-effects.md`、`docs/visible-presets.md`
- 設計：`design-system/fxcanvas/MASTER.md`

---

*若要 git 正式存檔（commit），請明確說「幫我 commit」與希望的訊息風格。*
